<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.OptionsDao">

    <resultMap type="com.example.demo.entity.Options" id="OptionsMap">
        <result property="optionId" column="option_id" jdbcType="INTEGER"/>
        <result property="questionId" column="question_id" jdbcType="INTEGER"/>
        <result property="option" column="option" jdbcType="VARCHAR"/>
        <result property="isCorrect" column="is_correct" jdbcType="INTEGER"/>
        <result property="orderNumber" column="order_number" jdbcType="INTEGER"/>

        <association property="questions" javaType="com.example.demo.entity.Questions">
            <id property="questionId" column="q_question_id" jdbcType="INTEGER" />
            <result property="question" column="question" jdbcType="VARCHAR" />
            <result property="questionTypeId" column="question_type_id" jdbcType="INTEGER" />
            <result property="picturePath" column="picture_path" jdbcType="VARCHAR" />
            <result property="videoPath" column="video_path" jdbcType="VARCHAR" />
            <result property="audioPath" column="audio_path" jdbcType="VARCHAR" />
            <result property="questionOrder" column="question_order" jdbcType="INTEGER" />
            <result property="explanation" column="explanation" jdbcType="VARCHAR" />
        </association>

    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="OptionsMap">
        SELECT
            o.option_id,
            o.question_id,
            o.option,
            o.is_correct,
            o.order_number,
            q.question_id AS q_question_id,
            q.question,
            q.question_type_id,
            q.picture_path,
            q.video_path,
            q.audio_path,
            q.question_order,
            q.explanation
        FROM options o
                 INNER JOIN questions q ON o.question_id = q.question_id
        WHERE o.option_id = #{optionId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="OptionsMap">
        SELECT
        o.option_id,
        o.question_id,
        o.option,
        o.is_correct,
        o.order_number,
        q.question_id AS q_question_id,
        q.question,
        q.question_type_id,
        q.picture_path,
        q.video_path,
        q.audio_path,
        q.question_order,
        q.explanation
        FROM options o
        INNER JOIN questions q ON o.question_id = q.question_id
        <where>
            <if test="options.optionId != null">
                and o.option_id = #{options.optionId}
            </if>
            <if test="options.questionId != null">
                and o.question_id = #{options.questionId}
            </if>
            <if test="options.option != null and options.option != ''">
                and o.option = #{options.option}
            </if>
            <if test="options.isCorrect != null">
                and o.is_correct = #{options.isCorrect}
            </if>
            <if test="options.orderNumber != null">
                and o.order_number = #{options.orderNumber}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from options
        <where>
            <if test="optionId != null">
                and option_id = #{optionId}
            </if>
            <if test="questionId != null">
                and question_id = #{questionId}
            </if>
            <if test="option != null and option != ''">
                and option = #{option}
            </if>
            <if test="isCorrect != null">
                and is_correct = #{isCorrect}
            </if>
            <if test="orderNumber != null">
                and order_number = #{orderNumber}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="optionId" useGeneratedKeys="true">
        insert into options(question_id, 'option', is_correct, order_number)
        values (#{questionId}, #{option}, #{isCorrect}, #{orderNumber})
    </insert>

    <insert id="insertBatch" keyProperty="optionId" useGeneratedKeys="true">
        insert into options(question_id, 'option', is_correct, order_number)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.questionId}, #{entity.option}, #{entity.isCorrect}, #{entity.orderNumber})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="optionId" useGeneratedKeys="true">
        insert into options(question_id, 'option', is_correct, order_number)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.questionId}, #{entity.option}, #{entity.isCorrect}, #{entity.orderNumber})
        </foreach>
        on duplicate key update
        question_id = values(question_id),
        'option' = values(option),
        is_correct = values(is_correct),
        order_number = values(order_number)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update options
        <set>
            <if test="questionId != null">
                question_id = #{questionId},
            </if>
            <if test="option != null and option != ''">
                option = #{option},
            </if>
            <if test="isCorrect != null">
                is_correct = #{isCorrect},
            </if>
            <if test="orderNumber != null">
                order_number = #{orderNumber},
            </if>
        </set>
        where option_id = #{optionId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from options where option_id = #{optionId}
    </delete>

</mapper>

