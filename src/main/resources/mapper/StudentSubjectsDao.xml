<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.StudentSubjectsDao">

    <resultMap type="com.example.demo.entity.StudentSubjects" id="StudentSubjectsMap">
        <result property="studentSubjectId" column="student_subject_id" jdbcType="INTEGER"/>
        <result property="studentId" column="student_id" jdbcType="INTEGER"/>
        <result property="subjectId" column="subject_id" jdbcType="INTEGER"/>
        <result property="progress" column="progress" jdbcType="INTEGER"/>

        <!-- 关联StudentUser对象 -->
        <association property="student" javaType="com.example.demo.entity.StudentUser">
            <id property="id" column="student_id"/>
            <result property="username" column="username"/>
            <result property="password" column="password"/>
            <result property="email" column="email"/>
        </association>

        <!-- 关联Subjects对象 -->
        <association property="subject" javaType="com.example.demo.entity.Subjects">
            <id property="subjectId" column="subject_id"/>
            <result property="subjectName" column="subject_name"/>
            <result property="level" column="level"/>
            <result property="description" column="description"/>
        </association>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StudentSubjectsMap">
        select
          student_subject_id, student_id, subject_id, progress
        from student_subjects
        where student_subject_id = #{studentSubjectId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="StudentSubjectsMap">
        select
          student_subject_id, student_id, subject_id, progress
        from student_subjects
        <where>
            <if test="studentSubjectId != null">
                and student_subject_id = #{studentSubjectId}
            </if>
            <if test="studentId != null">
                and student_id = #{studentId}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="progress != null">
                and progress = #{progress}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from student_subjects
        <where>
            <if test="studentSubjectId != null">
                and student_subject_id = #{studentSubjectId}
            </if>
            <if test="studentId != null">
                and student_id = #{studentId}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="progress != null">
                and progress = #{progress}
            </if>
        </where>
    </select>


    <!--新增所有列-->
    <insert id="insert" keyProperty="studentSubjectId" useGeneratedKeys="true">
        insert into student_subjects(student_id, subject_id, progress)
        values (#{studentId}, #{subjectId}, #{progress})
    </insert>

    <insert id="insertBatch" keyProperty="studentSubjectId" useGeneratedKeys="true">
        insert into student_subjects(student_id, subject_id, progress)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.studentId}, #{entity.subjectId}, #{entity.progress})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="studentSubjectId" useGeneratedKeys="true">
        insert into student_subjects(student_id, subject_id, progress)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.studentId}, #{entity.subjectId}, #{entity.progress})
        </foreach>
        on duplicate key update
        student_id = values(student_id),
        subject_id = values(subject_id),
        progress = values(progress)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update student_subjects
        <set>
            <if test="studentId != null">
                student_id = #{studentId},
            </if>
            <if test="subjectId != null">
                subject_id = #{subjectId},
            </if>
            <if test="progress != null">
                progress = #{progress},
            </if>
        </set>
        where student_subject_id = #{studentSubjectId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from student_subjects where student_subject_id = #{studentSubjectId}
    </delete>

    <select id="queryByStudentId" resultType="com.example.demo.entity.StudentSubjects">
        SELECT
            ss.student_subject_id as studentSubjectId,
            ss.student_id as studentId,
            ss.subject_id as subjectId,
            ss.progress as progress,
            su.id as "student.id",
            su.username as "student.username",
            su.email as "student.email",
            s.subject_id as "subject.subjectId",
            s.subject_name as "subject.subjectName",
            s.level as "subject.level",
            s.description as "subject.description"
        FROM student_subjects ss
                 LEFT JOIN student_user su ON ss.student_id = su.id
                 LEFT JOIN subjects s ON ss.subject_id = s.subject_id
        WHERE ss.student_id = #{studentId}
    </select>



</mapper>

