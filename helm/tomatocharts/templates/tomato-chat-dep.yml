apiVersion: apps/v1
kind: Deployment
metadata:
  name: tchat
  labels: 
    app: tchat
spec:
  replicas: 4
  selector:
    matchLabels:
      app: tchat
  template:
    metadata:
      labels:
        app: tchat
    spec:
      containers:
      - name: tchat
        image: {{ .Values.appimage}}
        ports:
        - name: tchat-port
          containerPort: 5000
